.playwright-mcp/
.claude/settings.local.json
__pycache__/
*.pyc
.venv/

# Go build + test artifacts
/ana
/bin/
/dist/
*.test
*.out
coverage.out
c.out
c.internal.out

# e2e manual-revert ledger files (written by the live smoke harness when
# auto-cleanup fails and human intervention is required)
e2e-manual-revert-*.md

# Secrets
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Claude Code personal overrides
CLAUDE.local.md
.claude/scheduled_tasks.lock

# Anonymizer identity-token table — contains real names we never want in git.
# See scripts/CLAUDE.md for contents/shape.
.claude/skills/textql-webapp-probe/scripts/identity_tokens.local.py

# CodeGraph — local-only semantic index
.codegraph/
